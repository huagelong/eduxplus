{% extends '@WebsiteBundle/layout/layout.html.twig' %}
 {% block page %}
   {{ parent() }}
<div id="main" class="commonMain">
  <div class="layui-main helpCard">
    <div class="layui-row layui-col-space20">
      <div class="layui-col-md2">
        {{ render(path('app_help_nav', {id:id,type:type})) }}
      </div>
      <div class="layui-col-md10" >
        <div class="mainBox">
          <div class="content">
        <div class="layui-card helpHeader">
          <div class="layui-card-header layui-center">{{ category['name'] }}</div>
          <div class="layui-card-body helpBody">
            {% if details %}
            <ul>
                {% for dv in details %}
              <li>
                <a href="{{ path('app_help', {"type":0, "id":dv['id']}) }}"> {{ loop.index }}、{{ dv['name'] }}</a>
              </li>
                {% endfor %}
            </ul>
              {% else %}
                  <div class="layui-center">
                    <div><img src="/bundles/eduxpluswebsite/default/images/null-i3.png" class="blank-img" /></div>
                    <div>没有数据～</div>
                  </div>
              {% endif %}
          </div>
        </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
